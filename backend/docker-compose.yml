services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: storetrack
      POSTGRES_PASSWORD: storetrack
      POSTGRES_DB: storetrack
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
